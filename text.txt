<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For You</title>
    <style>
        :root {
            --primary-pink: #ff4d6d;
            --bg-color: #fff0f3;
        }

        body, html {
            margin: 0; padding: 0;
            width: 100%; height: 100%;
            overflow: hidden;
            font-family: 'Georgia', serif;
            background-color: var(--bg-color);
        }

        /* Rose Petal Container */
        #petal-container {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .petal {
            position: absolute;
            background-color: #ffb3c1;
            border-radius: 150% 0 150% 0;
            opacity: 0.8;
            animation: fall linear infinite;
        }

        @keyframes fall {
            0% { transform: translateY(-10%) rotate(0deg); }
            100% { transform: translateY(110vh) rotate(360deg); }
        }

        /* Main UI */
        .content-wrapper {
            position: relative;
            z-index: 10;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            padding: 20px;
            box-sizing: border-box;
        }

        .card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(5px);
            padding: 2rem;
            border-radius: 25px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(255, 77, 109, 0.2);
            width: 100%;
            max-width: 350px;
        }

        .page { display: none; animation: fadeIn 0.5s ease-in; }
        .page.active { display: block; }

        h2 { color: #800e13; font-size: 1.5rem; line-height: 1.4; }

        .btn-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 25px;
            align-items: center;
        }

        button {
            width: 80%;
            padding: 15px;
            border: none;
            border-radius: 30px;
            font-size: 1.1rem;
            font-weight: bold;
            transition: 0.3s;
            touch-action: manipulation;
        }

        .btn-yes { background: var(--primary-pink); color: white; }
        #no-btn { background: #dfdfdf; color: #666; position: relative; }

        @keyframes fadeIn {
            from { opacity: 0; } to { opacity: 1; }
        }

        .para { font-style: italic; color: #590d22; line-height: 1.6; }
    </style>
</head>
<body>

    <audio id="bg-music" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>

    <div id="petal-container"></div>

    <div class="content-wrapper">
        <div class="page active" id="page0">
            <h2>I have a surprise for you...</h2>
            <div class="btn-container">
                <button class="btn-yes" onclick="startExperience()">Open it ❤️</button>
            </div>
        </div>

        <div class="page" id="page1">
            <h2>Be honest...<br>Do you think you are beautiful?</h2>
            <div class="btn-container">
                <button class="btn-yes" onclick="nextPage(2)">Yes</button>
                <button id="no-btn" ontouchstart="moveNo()" onmouseover="moveNo()">No</button>
            </div>
        </div>

        <div class="page" id="page2">
            <h2>Do you know how much your smile lights up my world?</h2>
            <div class="btn-container">
                <button class="btn-yes" onclick="nextPage(3)">Maybe a little?</button>
                <button class="btn-yes" onclick="nextPage(3)">Now I do!</button>
            </div>
        </div>

        <div class="page" id="page3">
            <h2>Ready for the truth?</h2>
            <div class="btn-container">
                <button class="btn-yes" onclick="nextPage(4)">Tell me</button>
            </div>
        </div>

        <div class="page" id="page4">
            <h2>The Truth...</h2>
            <p class="para">
                You have a beauty that words struggle to capture. It’s not just the way you look—though you are absolutely breathtaking—it’s the kindness in your soul. You are a masterpiece of grace and light, and I hope you see yourself through my eyes just for one second, so you could finally understand how truly extraordinary you are.
            </p>
        </div>
    </div>

    <script>
        function startExperience() {
            document.getElementById('bg-music').play();
            createPetals();
            nextPage(1);
        }

        function nextPage(num) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            document.getElementById('page' + num).classList.add('active');
        }

        function moveNo() {
            const btn = document.getElementById('no-btn');
            const x = Math.random() * 150 - 75;
            const y = Math.random() * 150 - 75;
            btn.style.transform = `translate(${x}px, ${y}px)`;
        }

        function createPetals() {
            const container = document.getElementById('petal-container');
            for (let i = 0; i < 30; i++) {
                const petal = document.createElement('div');
                petal.className = 'petal';
                petal.style.left = Math.random() * 100 + 'vw';
                petal.style.width = Math.random() * 15 + 10 + 'px';
                petal.style.height = petal.style.width;
                petal.style.animationDuration = Math.random() * 3 + 2 + 's';
                petal.style.delay = Math.random() * 5 + 's';
                container.appendChild(petal);
            }
        }
    </script>
</body>
</html>
